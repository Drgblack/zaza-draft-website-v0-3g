"use client";
import { ReactNode } from "react";

export default function LanguageProvider({ children }: { children: ReactNode }) {
  // Add i18n/context here later if needed
  return <>{children}</>;
}
